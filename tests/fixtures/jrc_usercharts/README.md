Test dataset for JRC userchart reverse engineering.

Naming:
R = roundtrip tests
T = text behavior
L = line behavior
N = native object comparison

Purpose:
- Infer JRC CSV structure
- Validate exporter compatibility
- Compare .uchm vs .csv behavior